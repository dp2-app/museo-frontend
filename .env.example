VITE_API_BASE_URL=http://localhost:8000/api/v1

# Subida directa de fotografías (RF-013). Requiere un upload preset "unsigned"
# creado en el panel de Cloudinary (Settings → Upload → Upload presets).
# Si se dejan vacíos, la ficha de pieza cae de vuelta a pegar la URL manualmente.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
